export const pointValues = [1, 2, 3, 4, 5];

export const possibleGrades = [2, 3, 4, 5, 6];

// sorted alphabetically
// keep syncronyzed with account builder in db/accounts.ts
export const allRoles = ['admin', 'student', 'teacher'];

export default {
  pointValues,
  possibleGrades,
  allRoles,
};
